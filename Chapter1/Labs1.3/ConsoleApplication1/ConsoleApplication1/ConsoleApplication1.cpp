#include "stdafx.h"
#include <iostream>//we included iostream
#include <iomanip>//we included iomanip
#include <string>//we included string

using namespace std;
int main()
{
	int hours = 10800; // 3*60*60
	int three = 3 * 60;// This is a variable to hold the value of 3 minutes in seconds
	int five = 5 * 60;// This is a variable to hold the value of 6 minutes in seconds
	double pi = 3.141526; //This is the value of pi
	cout << "result: " << hours << endl;
	// here we should print the v value but a programmer didn't have time to write any code
	// but h
}